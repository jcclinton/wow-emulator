% network defines
-define(I, /unsigned-little-integer).
-define(O, /unsigned-big-integer).
-define(b, /bytes).
-define(f, :32/float-little).
-define(SH, :160?I). % sha1
-define(Q, :64?I). % quad
-define(L, :32?I). % long
-define(W, :16?I). % word
-define(B, :8). %byte
